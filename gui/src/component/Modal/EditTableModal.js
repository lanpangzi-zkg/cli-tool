import React, { PureComponent } from 'react';
import { Button, Modal, Form } from 'antd';

class EditTableModal extends PureComponent {
    render() {
        return (
            <Modal>
                <Form>
                    
                </Form>
            </Modal>
        );
    }
}

export default Form.create()(EditTableModal);